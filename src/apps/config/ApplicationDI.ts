import { AppService } from '../../application/services/health/app.service';
import { AppController } from '../../presentation/controllers/health/app.controller';

export const controllers = [AppController];
export const services = [AppService];
export const repositories = [];
